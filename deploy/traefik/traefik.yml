api:
  dashboard: false

entryPoints:
  web:
    address: ":80"
  websecure:
    address: ":443"

providers:
  docker:
    endpoint: unix:///var/run/docker.sock
    exposedByDefault: false
    network: satellite_public
  file:
    directory: /etc/traefik/dynamic
    watch: true

log:
  level: INFO
