"""Доменные константы календарного слоя.

Эти строки используются и для фильтрации событий (``events.is_lunch_event``),
и для пользовательских текстов (``messages_ru``). Раньше они жили в
``messages_ru``, и календарь зависел от слоя UI — нарушение направления слоёв.
``messages_ru`` теперь реэкспортирует их для обратной совместимости.
"""

from __future__ import annotations

# Маркер для скрытия событий «приёма пищи» из основного расписания.
# Сейчас триггер — эмодзи «пицца» в названии; ключевое слово (завтрак/обед/ужин)
# выбирает конкретный шаблон в render-слое.
LUNCH_EMOJI_MARKER = "🍕"
LUNCH_TEXT_MARKER = "обед"

# Подпись для all-day событий в плане (когда они не скрыты HIDE_ALL_DAY_EVENTS).
PLAN_ALL_DAY_LABEL = "весь день"
