name: tests

# Гейт перед merge: только pull_request. Push в main — workflow deploy
# (вызывает тот же reusable checks → build → deploy).

on:
  pull_request:

jobs:
  checks:
    uses: ./.github/workflows/_checks.yml
